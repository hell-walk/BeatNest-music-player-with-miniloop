window.addEventListener("DOMContentLoaded", () => {
    const container = document.querySelector(".signup-container");
    setTimeout(() => {
      container.classList.add("visible");
    }, 200); // Delay before the container rises and glows
  });